Let’s do this: 

* - create a local repository called uj-git-start,
* - and add two files to it,
* - commit  the files into  repo,
* - let’s create  a branch  called say-hello,
* - lets change into the branch 
* and create a file in the branch,
* add and commit to the branch,
* merge the branch into main.
